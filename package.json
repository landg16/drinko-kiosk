{
  "name": "drinko-kiosk",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "build": "pnpm -r run build",
    "format": "pnpm -r format"
  },
  "devDependencies": {
    "prettier": "^3.4.2"
  }
}
